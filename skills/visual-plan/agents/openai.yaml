interface:
  display_name: "Visual Plan"
  short_description: "Approve, implement, and diff validated visual plans"
  default_prompt: "Use $visual-plan to validate and import a code plan, wait for module approval, implement approved scope, and verify drift."
